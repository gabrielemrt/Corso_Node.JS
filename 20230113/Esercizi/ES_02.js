// Scrivere una funzione che dato un numero restituisca la radice quadrata

var number = function(num) {

    return Math.sqrt(num);

}

n = 4;
console.log('La radice quadrata di ' + n, 'è ' + number(n));